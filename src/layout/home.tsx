import styles from './home.module.scss';

const Home: React.FC = () => {
  return (
    <main className={styles.main}>
      <div className={styles.todo}>Main</div>
    </main>
  );
};

export default Home;